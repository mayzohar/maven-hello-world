apiVersion: v1
kind: Service
metadata:
  name: {{ include "mavem-app.fullname" . }}
  labels:
    {{- include "mavem-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mavem-app.selectorLabels" . | nindent 4 }}
