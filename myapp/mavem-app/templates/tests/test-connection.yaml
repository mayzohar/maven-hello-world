apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mavem-app.fullname" . }}-test-connection"
  labels:
    {{- include "mavem-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mavem-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
